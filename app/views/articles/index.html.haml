%h1{style: "text-align:left;"} Edilnews 
%table#articles
 %thead
  %tr
   %th Titolo Articolo
   %th Data Realizzazione
   %th Testo
   %th Autore
   %th Maggiori info
 %tbody
  - @all.each do |article|
   %tr
    %td=article.title
    %td=article&.release_date&.strftime("%B %d %Y")
    %td=article.text
    %td=article.author
    %td=link_to "Dettagli", article_path(article)
    %td= article.likes.count
    %td= (article.likes.count) == 1 ? 'Like' : 'Likes'
%br
%h4 Ricerca per titolo
= form_tag search_path, :method => :get do
 = text_field_tag(:search, params[:search], placeholder: "Type", class: "form-control")
 = submit_tag 'Cerca articolo', :class => 'btn btn-info glyphicon glyphicon-search' , :title => nil
%br
%nav.navbar.navbar-default
 .containter-fluid
  .navbar-header
   %button.navbar-toggle.collapsed{ type: :button, 'data-toggle' => :collapse, 'data-target' => "#bs-example-navbar-collapse-1" }
    %span.sr-only Toggle Navigation
    %span.icon-bar
    %span.icon-bar
    %span.icon-bar
    = link_to 'Crea Articolo', new_article_path, class: 'navbar-brand'