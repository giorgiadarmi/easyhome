%h2 Articoli preferiti

- if @articles.empty?
    %p No article saved as favorite
- else
    %br
    - @articles.each do |c|
        = link_to '#{c.title}', article_path(c.id), :class => 'btn btn-outline-success'
        = link_to 'Remove from Favorites', favorite_article_path(c, type: 'unfavorite'), :method => :put
        %br
        %br
    
= link_to 'Torna a edilnews', articles_path
